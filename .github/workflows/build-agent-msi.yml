name: Build Remote Management Agent MSI

# Produces DeployCoreRemoteAgent.msi on a real Windows runner (it can't be built
# on the Linux hosts DeployCore otherwise uses) and publishes it so DeployCore
# can auto-fetch it (see backend app/services/remote_agent_seed.py).
#
# Runs automatically whenever the agent's own files change on main, so a normal
# `git push` is enough - no tag needed. It keeps a rolling "agent-latest"
# pre-release whose asset URL is stable
# (…/releases/download/agent-latest/DeployCoreRemoteAgent.msi), which is what
# the README, Wiki, and the DeployCore auto-seed all point at. Pushing a real
# `agent-v*` tag additionally publishes a normal versioned release.
on:
  push:
    branches: [main]
    paths:
      - "backend/app/services/remote_agent_install.ps1"
      - "remote-agent/wix/**"
      - "remote-agent/agent/**"
      - ".github/workflows/build-agent-msi.yml"
    tags:
      - "agent-v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # The MSI wraps the SAME script the running server serves for the
      # one-liner install - copy it in so there's a single source of truth.
      - name: Stage installer script
        shell: pwsh
        run: Copy-Item backend/app/services/remote_agent_install.ps1 remote-agent/wix/Install-DeployCoreAgent.ps1

      # Build the agent itself: a self-contained, single-file win-x64 publish
      # (no separate .NET runtime install needed on the target machine - a
      # freshly provisioned VM may not have one). See remote-agent/agent/ and
      # remote-agent/PROTOCOL.md.
      - name: Build DeployCoreAgent
        shell: pwsh
        run: |
          dotnet publish remote-agent/agent/DeployCoreAgent.csproj -c Release -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true `
            -o remote-agent/wix
          if (-not (Test-Path remote-agent/wix/DeployCoreAgent.exe)) { throw "DeployCoreAgent.exe was not produced" }

      # Static Windows ffmpeg build (BtbN's builds are the commonly-used
      # source for this - GPL build includes libx264, which is what the
      # agent actually needs; see PROTOCOL.md's capture/encode pipeline).
      # Pinned tag, not "latest", for the same reason the old RustDesk
      # version was pinned: a future upstream release shouldn't change
      # install behavior under us without a deliberate bump here.
      - name: Download ffmpeg
        shell: pwsh
        run: |
          $tag = "autobuild-2024-12-20-12-54"
          Invoke-WebRequest -Uri "https://github.com/BtbN/FFmpeg-Builds/releases/download/$tag/ffmpeg-master-latest-win64-gpl.zip" -OutFile ffmpeg.zip -UseBasicParsing
          Expand-Archive -Path ffmpeg.zip -DestinationPath ffmpeg-extracted -Force
          $exe = Get-ChildItem -Path ffmpeg-extracted -Recurse -Filter ffmpeg.exe | Select-Object -First 1
          if (-not $exe) { throw "ffmpeg.exe not found in downloaded archive" }
          Copy-Item $exe.FullName remote-agent/wix/ffmpeg.exe
          if (-not (Test-Path remote-agent/wix/ffmpeg.exe)) { throw "ffmpeg download failed" }

      # Build the branded tray companion, then use it to emit the DeployCore
      # .ico (drawn with GDI+, so no ImageMagick/Inkscape needed). Both get
      # staged next to the .wxs for the MSI to bundle.
      - name: Build tray app + icon
        shell: pwsh
        run: |
          dotnet build remote-agent/tray/DeployCoreTray.csproj -c Release -o remote-agent/tray/bin
          Start-Process -FilePath remote-agent/tray/bin/DeployCoreTray.exe -ArgumentList '--export-icon','deploycore.ico' -WorkingDirectory remote-agent/wix -Wait
          Copy-Item remote-agent/tray/bin/DeployCoreTray.exe remote-agent/wix/DeployCoreTray.exe
          if (-not (Test-Path remote-agent/wix/deploycore.ico)) { throw "icon was not generated" }

      # Pinned to WiX 5: WiX 6+/7 gate behind the paid Open Source Maintenance
      # Fee EULA (error WIX7015), which can't be accepted non-interactively in
      # CI. WiX 5 is the last MIT/free line and uses the same v4 schema + the
      # Wix4UtilCA_X64/WixQuietExec64 custom-action reference this .wxs uses.
      - name: Install WiX
        shell: pwsh
        run: dotnet tool install --global wix --version 5.0.2

      - name: Add WiX UtilExtension
        shell: pwsh
        run: wix extension add -g WixToolset.Util.wixext/5.0.2

      # A static ProductVersion was a real, confirmed bug: Windows Installer
      # treats a repeat `msiexec /i` of what it considers "the same product,
      # same version" as a no-op/repair, not a fresh install - MajorUpgrade
      # (below) only fires on an actual version increase. run_number always
      # goes up, so every published build is guaranteed installable over
      # whatever was there before, even a same-content-different-build case.
      - name: Build MSI
        shell: pwsh
        working-directory: remote-agent/wix
        run: wix build DeployCoreRemoteAgent.wxs -ext WixToolset.Util.wixext -arch x64 -d BuildVersion="1.0.${{ github.run_number }}.0" -o DeployCoreRemoteAgent.msi

      # The one-liner install path (no .msi, just the PowerShell script) has
      # no bundled payload of its own - it downloads this zip the same way
      # the RustDesk-based version used to download rustdesk-x86_64.msi
      # directly (see remote_agent_install.ps1 step 2).
      - name: Package one-liner agent bundle
        shell: pwsh
        working-directory: remote-agent/wix
        run: Compress-Archive -Path DeployCoreAgent.exe, ffmpeg.exe -DestinationPath DeployCoreAgent.zip -Force

      # Smoke test: install with SKIPRUN=1, which still runs RegisterAgentTask
      # (only RunAgentTask, the one that would need a live server, is gated
      # off) - so this exercises the exact schtasks /create command, including
      # its nested quoting of SERVERURL/ENROLLTOKEN, that a real install
      # depends on. Confirms the task's stored command actually contains the
      # values it was given, catching a quoting mistake here instead of on a
      # real deployment. Then cleans up and uninstalls.
      - name: Smoke test install
        shell: pwsh
        working-directory: remote-agent/wix
        run: |
          $msi = "DeployCoreRemoteAgent.msi"
          $testUrl = "http://smoketest.example.com:8000"
          $testToken = "smoketesttoken123"
          $p = Start-Process msiexec.exe -ArgumentList "/i", "`"$msi`"", "/qn", "SKIPRUN=1", "SERVERURL=`"$testUrl`"", "ENROLLTOKEN=$testToken", "/l*v", "install.log" -Wait -PassThru
          if ($p.ExitCode -ne 0) {
              # The last 40 lines are just the engine's generic property-dump
              # trailer, not the actual cause (confirmed - it hid the real
              # error entirely on the first run of this smoke test). Print
              # every custom-action start/end/return line, which is where the
              # real failure shows, plus a much bigger tail as a fallback.
              Write-Host "--- custom action trace ---"
              Get-Content install.log | Select-String "^Action start|^Action ended|Return value|CustomAction|Error \d|^MSI \(c\)" | ForEach-Object { Write-Host $_.Line }
              Write-Host "--- last 200 lines of install.log ---"
              Get-Content install.log -Tail 200
              throw "MSI install failed with exit code $($p.ExitCode)"
          }
          $installDir = Join-Path ${env:ProgramFiles} "DeployCore Remote Management Agent"
          $script = Join-Path $installDir "Install-DeployCoreAgent.ps1"
          $runCmd = Join-Path $installDir "RunAgentInstall.cmd"
          $bundledAgent = Join-Path $installDir "DeployCoreAgent.exe"
          $bundledFfmpeg = Join-Path $installDir "ffmpeg.exe"
          $paramsIni = Join-Path $installDir "agent-params.ini"
          if (-not (Test-Path $script)) { throw "Installer did not place the agent script at $script" }
          if (-not (Test-Path $runCmd)) { throw "Installer did not place the task launcher at $runCmd" }
          if (-not (Test-Path $bundledAgent)) { throw "Installer did not bundle the agent at $bundledAgent" }
          if (-not (Test-Path $bundledFfmpeg)) { throw "Installer did not bundle ffmpeg at $bundledFfmpeg" }

          # SERVERURL/ENROLLTOKEN are written via WiX's own IniFile action, not
          # passed as Scheduled Task command-line arguments - deliberately, so
          # there's nothing for Task Scheduler to mis-parse at actual execution
          # time (a real deployment showed exactly that gap: the agent script
          # produced no log at all, consistent with an empty/lost enroll
          # token). Verify the file exists and both values actually landed.
          if (-not (Test-Path $paramsIni)) { throw "Installer did not write $paramsIni" }
          $paramsContent = Get-Content $paramsIni -Raw
          Write-Host "agent-params.ini content:`n$paramsContent"
          if ($paramsContent -notmatch [regex]::Escape($testUrl)) { throw "agent-params.ini is missing the SERVERURL value: $paramsContent" }
          if ($paramsContent -notmatch [regex]::Escape($testToken)) { throw "agent-params.ini is missing the ENROLLTOKEN value: $paramsContent" }

          $taskLauncher = Join-Path $env:ProgramData "DeployCore\run.cmd"
          $agentLog = Join-Path $env:ProgramData "DeployCore\agent-install.log"
          if (-not (Test-Path $taskLauncher)) { throw "TaskLauncher component did not place the no-space launcher at $taskLauncher" }

          $taskInfo = & schtasks.exe /query /tn DeployCoreAgentInstall /fo list /v 2>&1
          if ($LASTEXITCODE -ne 0) { throw "RegisterAgentTask did not register the scheduled task: $taskInfo" }
          $taskToRun = ($taskInfo | Select-String "^Task To Run:") -join "`n"
          Write-Host "Registered task command: $taskToRun"
          if ($taskToRun -notmatch [regex]::Escape($taskLauncher)) { throw "Scheduled task command doesn't reference $taskLauncher - quoting likely broke: $taskToRun" }

          # Actually EXECUTE the registered task, not just check its stored
          # command text - confirmed live as a real, previously-missed gap:
          # the stored text can look completely correct via `schtasks
          # /query /fo list` while Task Scheduler's own internal XML has
          # silently split a quoted, space-containing /tr path at the first
          # space (`schtasks /query ... /xml` on a real failing install
          # showed exactly this: <Command>C:\Program</Command><Arguments>
          # Files\...\RunAgentInstall.cmd</Arguments>), which only shows up
          # by actually trying to run it (result 0x80070002, file not
          # found) - SKIPRUN=1 never exercised this at all before. The
          # fake server address means the script itself will fail quickly
          # once it actually starts (no real DeployCore to call) - that's
          # fine, this only checks that Task Scheduler could FIND and
          # LAUNCH the chain at all the way through to the real script
          # (proven by agent-install.log existing - Start-Transcript is
          # the very first thing Install-DeployCoreAgent.ps1 does, before
          # any validation that could fail first).
          & schtasks.exe /run /tn DeployCoreAgentInstall | Out-Null
          $launched = $false
          for ($i = 0; $i -lt 15; $i++) {
              Start-Sleep -Seconds 2
              if (Test-Path $agentLog) { $launched = $true; break }
          }
          if (-not $launched) {
              $finalTaskInfo = & schtasks.exe /query /tn DeployCoreAgentInstall /fo list /v 2>&1
              Write-Host "Task state after /run: $finalTaskInfo"
              throw "Scheduled task did not actually launch the agent script (no $agentLog after 30s) - Task Scheduler could register the task but not execute it, likely the same /tr quoting-loss bug this test exists to catch"
          }
          Write-Host "Scheduled task executed successfully (agent-install.log was created)."

          & schtasks.exe /delete /tn DeployCoreAgentInstall /f | Out-Null
          Remove-Item $agentLog -Force -ErrorAction SilentlyContinue

          # Repeat-install check: catches a real, previously-shipped bug where
          # RegisterAgentTask/RunAgentTask were conditioned on "NOT Installed"
          # instead of "NOT REMOVE" - `Installed` is set on ANY repeat install
          # of a product Windows Installer already has a record of (not just
          # uninstalls), so a second `msiexec /i` of the same product silently
          # skipped both actions with no error at all - exactly what a user
          # retrying a failed/manually-cleaned-up install would hit. Confirms
          # the task gets registered again on a plain repeat install, not just
          # on the very first one.
          $p2 = Start-Process msiexec.exe -ArgumentList "/i", "`"$msi`"", "/qn", "SKIPRUN=1", "SERVERURL=`"$testUrl`"", "ENROLLTOKEN=$testToken", "/l*v", "reinstall.log" -Wait -PassThru
          if ($p2.ExitCode -ne 0) {
              Write-Host "--- repeat-install custom action trace ---"
              Get-Content reinstall.log | Select-String "^Action start|^Action ended|Return value|CustomAction|Error \d|^MSI \(c\)" | ForEach-Object { Write-Host $_.Line }
              throw "Repeat MSI install failed with exit code $($p2.ExitCode)"
          }
          $taskInfo2 = & schtasks.exe /query /tn DeployCoreAgentInstall /fo list /v 2>&1
          if ($LASTEXITCODE -ne 0) { throw "RegisterAgentTask did not re-register the scheduled task on a repeat install (this is the NOT Installed vs NOT REMOVE bug): $taskInfo2" }
          & schtasks.exe /delete /tn DeployCoreAgentInstall /f | Out-Null

          Start-Process msiexec.exe -ArgumentList "/x", "`"$msi`"", "/qn" -Wait
          Write-Host "Smoke test passed (including repeat-install)."

      # Always, pass or fail - a failure needs the full log to diagnose (the
      # console tail alone hid the real cause of the first real failure), and
      # keeping it on success too costs nothing and means it's already there
      # if a later, different failure needs it.
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: install-log
          path: |
            remote-agent/wix/install.log
            remote-agent/wix/reinstall.log
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v4
        with:
          name: DeployCoreRemoteAgent
          path: |
            remote-agent/wix/DeployCoreRemoteAgent.msi
            remote-agent/wix/DeployCoreAgent.zip

      # Rolling pre-release with a stable download URL - refreshed on every
      # qualifying push to main so DeployCore's auto-seed always finds a
      # build. Ships both assets: the .msi for the download button/deployment
      # pipeline, and the .zip the one-liner install path falls back to
      # downloading directly (see remote_agent_install.ps1 step 2).
      - name: Publish rolling agent-latest release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: agent-latest
          name: Remote Management Agent (latest)
          prerelease: true
          files: |
            remote-agent/wix/DeployCoreRemoteAgent.msi
            remote-agent/wix/DeployCoreAgent.zip

      # Versioned release when a real agent-v* tag is pushed.
      - name: Publish tagged release
        if: startsWith(github.ref, 'refs/tags/agent-v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            remote-agent/wix/DeployCoreRemoteAgent.msi
            remote-agent/wix/DeployCoreAgent.zip
